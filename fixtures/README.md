# Fixtures

`fixtures/sample/` contains one tiny public-safe fixture contract for local tests
and screenshots.

`fixtures/generated/` is where the fixture package writes deterministic generated
fixtures. Generated fixture artifacts should be recreated from commands, not
hand-edited.
